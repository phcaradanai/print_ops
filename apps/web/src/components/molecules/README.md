# Molecules

Molecules combine atoms into small reusable UI behaviors. They must stay independent of routes, API endpoints, permissions, and domain entities. Each molecule keeps its implementation, stylesheet, tests, and export in its own folder.
