# PageHeader

`PageHeader` is a presentation-only molecule for route identity and page-local actions.

The molecule owns responsive wrapping, title hierarchy, supporting copy, and the page action row. Pages continue to decide copy, permissions, and which actions are available.

The responsive breakpoint intentionally matches the existing application shell behavior at 1024px so the extraction does not change tablet layouts.
